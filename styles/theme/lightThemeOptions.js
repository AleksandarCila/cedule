import { ThemeOptions } from '@mui/material/styles';

const lightThemeOptions = {
  palette: {
    mode: 'light',

  },
  custom: {
    events: {
      event:'#1982c4',
      task: '#ffca3a',
      reminder: '#ff595e'
    }
  }
};

export default lightThemeOptions;
